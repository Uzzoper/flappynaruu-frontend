export interface Bird {
    x: number;
    y: number;
    width: number;
    height: number;
    velocity: number;
    frameIndex: number;
    frameTimer: number;
    hitboxOffsetX: number;
    hitboxOffsetY: number;
    hitboxWidth: number;
    hitboxHeight: number;
}